#include "CharacterVerification.h"

// 초기화
void CharacterVerification::Initialize(float CommuTime, float _Move_MarginofError)
{
}

// 이동 검증 함수
bool CharacterVerification::CharacterMoveVerificate(Vector3 _curPos, Vector3 _prePos, float _speed)
{
	return true;
}
